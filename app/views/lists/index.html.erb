<h1>Packing Lists</h1>

<%= button_to '+ New List', new_list_path %>

<table>
  <tr>
    <th>List</th>
    <th>Pack By</th>
  </tr>
  <% @lists.each do |list| %>
    <tr>
      <td><%= list.name %></td>
      <td><%= list.deadline_date %></td>
    <tr>
  <% end %>
</table>
